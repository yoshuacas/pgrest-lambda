# Design Prompts

This directory contains design prompts created by
`rring start` and feedback iterations created by
`rring iter`. Files follow the naming convention:

- `<feature>.md` — original feature prompt
- `<feature>-feedback-NNN.md` — feedback iterations
